# `@melosviz/brand-tokens`

Private design-system **token** package stub (WBS-P3.2 partial).

**Single source of truth:** [`desktop/assets/brand/tokens.css`](../../desktop/assets/brand/tokens.css)

`tokens.css` here only `@import`s the SoT (no forked hex/type values). Edit tokens
only in the desktop SoT. Full shared UI component library remains WBS-P3.2 / C10 L105.
