# MelosViz backend

Python package for MelosViz analysis, bridge, conductor, presets, and CLI.

```bash
pip install -e ".[test,lint,bridge,analysis]"
python -m melosviz.bridge.server --port 8765
```

See the [repo README](../README.md) and [`docs/`](../docs/).
