{
  "name": "melosviz-desktop",
  "version": "0.1.0",
  "description": "MelosViz native desktop app — Electrobun shell + Python backend sidecar",
  "private": true,
  "scripts": {
    "dev": "bunx electrobun dev",
    "build": "bunx electrobun build",
    "build:stable": "bunx electrobun build --env=stable",
    "package": "bunx electrobun package",
    "package:stable": "bunx electrobun package --env=stable",
    "test:e2e": "bun test tests/e2e_desktop.test.ts --timeout 60000",
    "test:e2e:bridge": "BRIDGE_ONLY=1 bun test tests/e2e_desktop.test.ts --timeout 60000"
  },
  "dependencies": {
    "@radix-ui/themes": "^3.1.0",
    "electrobun": "^1.18.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
