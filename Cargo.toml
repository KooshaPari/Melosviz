[workspace]
resolver = "2"
members = [
    # TODO(KP): re-enable once the upstream crate sources land — both
    # currently missing their src/ trees on this branch's base (pre-existing
    # repo breakage, not in production-delivery-extensions scope).
    # "crates/melosviz-render-wgpu",
    # "crates/melosviz-mir",
    "crates/melosviz-demo",
]
exclude = ["fuzz"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
