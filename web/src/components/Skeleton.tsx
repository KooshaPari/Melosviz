// Real implementation lives in the shared design-system package (C10 L105).
// Re-exported here so existing app imports (`./components/Skeleton`) keep
// working without churn across call-sites.
export { Skeleton, SkeletonBlock } from "@melosviz/ui";
