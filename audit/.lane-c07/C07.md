CLUSTER_START cluster=C07 repo=MelosViz pillars=L61-L70 date=2026-07-05

### L61 — Devcontainer / .devcontainer
score: 0  glyph: ✗
evidence:
  - ls -la at repo root shows no `.devcontainer/` directory
  - rg --files | rg "^(\\.devcontainer|devcontainer)" returned no matches
  - README.md:1-40 documents the app, but not a containerized one-command setup
gaps:
  - Add `.devcontainer/` with Dockerfile, devcontainer.json, and post-create bootstrap — effort: L
soft_goal_delta: not started

### L62 — Task runner
score: 1  glyph: △
evidence:
  - Makefile exists at repo root
  - rg --files | rg "^(Taskfile\\.yml|mise\\.toml|justfile)" returned no matches
  - README.md:20-60 explains manual build/test flows instead of a full task surface
gaps:
  - Replace the minimal Makefile with a broader task surface covering build/test/lint/format/bench — effort: M
soft_goal_delta: partially seeded; no unified runner

### L63 — EditorConfig + format-on-save
score: 0  glyph: ✗
evidence:
  - rg --files | rg "^(\\.editorconfig|\\.rustfmt\\.toml|\\.prettierrc)" returned no matches
  - repo root listing shows no formatting policy files
  - package manifests exist (Cargo.toml, backend/pyproject.toml, web/package.json) but no cross-language save-format config
gaps:
  - Add `.editorconfig` plus language format configs and CI enforcement — effort: M
soft_goal_delta: not started

### L64 — Test pyramid
score: 2  glyph: ~
evidence:
  - SPEC.md:574-610 defines pytest and vitest test coverage across backend and web
  - SPEC.md:595-597 references launcher-log invariants, implying integration-level coverage
  - SPEC.md:624 says no `cargo test` invocation in ci.yml, so the pyramid is split by stack rather than fully unified
gaps:
  - Add explicit e2e/chaos coverage markers and a single surfaced test-pyramid doc — effort: M
soft_goal_delta: unit+integration exist; e2e/chaos not clearly surfaced

### L65 — Mutation testing
score: 0  glyph: ✗
evidence:
  - rg -n --fixed-strings "mutant" --hidden --glob "!**/.git/**" . returned no repo matches in the visible evidence pass
  - rg -n --fixed-strings "mutpy" --hidden --glob "!**/.git/**" . returned no repo matches in the visible evidence pass
  - rg -n --fixed-strings "stryker" --hidden --glob "!**/.git/**" . returned no repo matches in the visible evidence pass
  - rg -n --fixed-strings "infection" --hidden --glob "!**/.git/**" . returned no repo matches in the visible evidence pass
  - .github/workflows and root manifests show no mutation-test harness or threshold gate
  - SPEC.md:582-586 discusses quality gates, but not mutation testing
gaps:
  - Add mutation testing for backend and web with a CI threshold gate — effort: M
soft_goal_delta: not started

### L66 — Property-based testing
score: 2  glyph: ~
evidence:
  - SPEC.md:353 references a seedable RNG for reproducibility
  - SPEC.md:636 names pytest-bdd for the video exporter flow, showing more than one test style in use
  - rg -n --fixed-strings "proptest" --hidden --glob "!**/.git/**" . produced no direct matches in the visible evidence pass
  - rg -n --fixed-strings "quickcheck" --hidden --glob "!**/.git/**" . produced no direct matches in the visible evidence pass
  - rg -n --fixed-strings "hypothesis" --hidden --glob "!**/.git/**" . produced no direct matches in the visible evidence pass
gaps:
  - Add explicit Hypothesis/proptest coverage for parser and model invariants — effort: M
soft_goal_delta: seeded design language exists; property-based tests are not explicit

### L67 — Fuzz harness
score: 0  glyph: ✗
evidence:
  - rg -n --fixed-strings "fuzz" --hidden --glob "!**/.git/**" . returned no repo matches in the visible evidence pass
  - rg -n --fixed-strings "cargo-fuzz" --hidden --glob "!**/.git/**" . returned no repo matches in the visible evidence pass
  - rg -n --fixed-strings "atheris" --hidden --glob "!**/.git/**" . returned no repo matches in the visible evidence pass
  - rg -n --fixed-strings "libFuzzer" --hidden --glob "!**/.git/**" . returned no repo matches in the visible evidence pass
  - Cargo workspace exists, but no fuzz harness directory or CI hook was surfaced in the scan
  - SPEC.md:624 notes no `cargo test` invocation in ci.yml, reinforcing the absence of fuzz-specific CI
gaps:
  - Add at least one fuzz harness for the parser/import surface and wire it into nightly CI — effort: M
soft_goal_delta: not started

### L68 — Flake detection
score: 1  glyph: △
evidence:
  - .qgate.toml exists at repo root, suggesting quality-gate orchestration
  - trace-gate.toml exists at repo root, suggesting gated repeatable checks
  - rg -n --fixed-strings "flake" --hidden --glob "!**/.git/**" . returned no dedicated flake-tracking system in the visible evidence pass
  - rg -n --fixed-strings "quarantine" --hidden --glob "!**/.git/**" . returned no dedicated flake-tracking system in the visible evidence pass
  - rg -n --fixed-strings "rerun" --hidden --glob "!**/.git/**" . returned no dedicated flake-tracking system in the visible evidence pass
gaps:
  - Add rerun stats / quarantine tracking for flaky tests and a root-cause log — effort: M
soft_goal_delta: gated checks exist; flake telemetry is not explicit

### L69 — Cross-platform CI
score: 2  glyph: ~
evidence:
  - SPEC.md:43 says the desktop shell targets native macOS/Windows app delivery
  - SPEC.md:658-672 describes macOS desktop packaging in release flow
  - SPEC.md:575-609 references platform-specific CI/test coverage across backend and web, but the visible scan did not show a complete linux+macos+windows matrix in root config
  - README.md:1-40 presents a multi-platform product surface, reinforcing cross-platform intent
gaps:
  - Make the CI matrix explicit for linux+macos+windows with platform-scoped jobs — effort: M
soft_goal_delta: cross-platform product intent is clear; CI matrix completeness is unclear

### L70 — Reproducible local dev
score: 1  glyph: △
evidence:
  - CLAUDE.md:1-20 gives environment guidance and mentions `source .venv/bin/activate`
  - README.md:20-60 outlines manual setup/build steps rather than a single self-verifying command
  - Makefile exists, but the visible scan did not show a `dev` one-liner or seed-data/bootstrap command
gaps:
  - Add a single command for local dev boot with verification and seeded sample data — effort: M
soft_goal_delta: boot guidance exists; one-command reproducibility does not

CLUSTER_TOTAL score=9/30 pct=30% grade=D
CLUSTER_DONE cluster=C07 repo=MelosViz
