CLUSTER_START cluster=C06 repo=MelosViz pillars=L51-L60 date=2026-07-05

### L51 — Pinned dependencies
score: 2  glyph: ~
evidence:
  - Cargo.lock:1 exists at repo root, pinning Rust dependencies
  - web/package-lock.json:1 exists, pinning the web bundle dependency graph
  - README.md:30-36 tells users to install from `backend/pyproject.toml`, but there is no lockfile for the Python package path
gaps:
  - No Python lockfile or pinned resolver output for the backend package — effort: M
soft_goal_delta: lockfiles exist for Rust and web, but Python dependency pinning is still partial

### L52 — Reproducible builds
score: 1  glyph: △
evidence:
  - .github/workflows/release.yml:95-106 builds release binaries with Cargo and packages them into tarballs
  - README.md:30-42 documents install and usage, but does not define a reproducibility or bit-identical release check
  - Cargo.toml:8 defines a release profile, but no reproducible-build controls are visible in repo docs or CI
gaps:
  - No SOURCE_DATE_EPOCH or release repro-check CI step — effort: M
soft_goal_delta: release binaries are built and packaged, but reproducibility is not validated

### L53 — Build provenance
score: 1  glyph: △
evidence:
  - .github/workflows/release.yml:116-141 publishes GitHub Releases after artifact download
  - .github/workflows/scorecard.yml:1-53 runs OSSF Scorecard with id-token write, showing supply-chain awareness
  - SECURITY.md:20-24 mentions scorecard-style security checks, but no build provenance attestation flow is defined for releases
gaps:
  - No SLSA/Sigstore attestation path for release artifacts — effort: L
soft_goal_delta: security scanning exists, but release provenance is not yet attested

### L54 — Hermetic builds
score: 0  glyph: ✗
evidence:
  - .github/workflows/release.yml:95-106 invokes `cargo build --release` directly with no network isolation or vendoring step
  - README.md:23-26 states FFmpeg must be on PATH or set via env var, which implies external runtime dependency resolution
  - backend/pyproject.toml:1-? and web/package-lock.json:1 show a mixed-language build surface without a hermetic build contract
gaps:
  - No CI-enforced no-network build or vendored dependency workflow — effort: L
soft_goal_delta: build inputs are documented, but the build is not hermetic

### L55 — Dependency confusion guard
score: 0  glyph: ✗
evidence:
  - No pyproject or package index policy file is visible at repo root governing alternate registries
  - No `.cargo/config.toml` registry scoping or private mirror policy appears in the repo tree
  - No dependency-confusion guidance is documented in CLAUDE.md:1-19 or README.md:1-134
gaps:
  - No scoped registry / mirror / typosquatting guardrail documented or enforced — effort: M
soft_goal_delta: not started

### L56 — Container provenance
score: 0  glyph: ✗
evidence:
  - No Dockerfile or OCI build workflow is present in the repo tree
  - .github/workflows/release.yml:82-141 ships tarballs, not signed container images
  - README.md:104-117 describes desktop and CLI usage only; no container distribution path is documented
gaps:
  - No container image signing, attestation, or deploy-time verification — effort: L
soft_goal_delta: not started

### L57 — MCP server provenance
score: 0  glyph: ✗
evidence:
  - README.md:11-12 says the supported workflow is library usage from Python; no MCP server is documented here
  - CLAUDE.md:8-12 is a stub for project layout and does not define any pinned service inventory
  - rg for `phenoMCP`, `MCP`, or server pinning in repo files does not show a provenance registry for external servers
gaps:
  - No pinned-and-versioned MCP server manifest or attestation trail — effort: M
soft_goal_delta: no server provenance surface is defined

### L58 — License scan
score: 1  glyph: △
evidence:
  - LICENSE:1-10 exists at repo root, so the project has a declared license
  - SECURITY.md:20-24 references security scanning practices, but no license-scanning workflow is visible
  - .github/workflows/scorecard.yml:1-53 covers supply-chain scorecarding, yet it does not enforce license allow/deny rules
gaps:
  - No every-PR license scan or copyleft deny gate — effort: M
soft_goal_delta: license is declared, but scan enforcement is absent

### L59 — Source code provenance
score: 0  glyph: ✗
evidence:
  - .github/workflows/release.yml:30-66 and :82-141 pin third-party actions by SHA, which helps workflow provenance
  - No branch policy or commit-signing policy is documented in README.md:1-134 or CLAUDE.md:1-19
  - No repo config for signed commits / required signatures is visible in the tree
gaps:
  - No commit-signing policy or branch protection rule for author provenance — effort: M
soft_goal_delta: workflow pins help, but source provenance is not enforced for authors

### L60 — Toolchain pinning
score: 1  glyph: △
evidence:
  - Cargo.toml:1-8 defines the Rust workspace entrypoint and release profile, but no rust-toolchain.toml is present
  - backend/pyproject.toml:1-? defines the Python package, but no .python-version or equivalent pin is present
  - README.md:20-36 documents Python 3.10+ and pip install steps, but not exact toolchain versions
gaps:
  - No pinned Rust/Python toolchain file or CI verification step — effort: M
soft_goal_delta: runtime requirements are stated, but exact toolchains are not pinned

CLUSTER_TOTAL score=5/30 pct=17% grade=F
CLUSTER_DONE cluster=C06 repo=MelosViz
